{
  "name": "@blockmanor/content",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Levels, story, economy tables — content is data (PRD §16). Level generator + balance harness are Stage-0 tooling (§5); shipped levels 1–60 are Stage 1 (§7.9).",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src test balance",
    "test": "vitest run",
    "balance": "vitest run --config vitest.balance.config.ts"
  },
  "dependencies": {
    "@blockmanor/engine": "workspace:*",
    "@blockmanor/shared": "workspace:*",
    "zod": "^3.24.1"
  }
}
